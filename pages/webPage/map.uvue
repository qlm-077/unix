<template>
	<view>
		<lm-Amap ref="amap" :zoom="18" style="width: 100%;height: 60%;"></lm-Amap>
	</view>
</template>

<script setup>
	import { LmAmapElement } from "uts.sdk.modules.lmAmap"
	import { locationInfo } from '@/store';
	const amap = ref<LmAmapElement | null>(null)
	// onMounted(() => {
	// 	let { latitude, longitude } = locationInfo.value!;
	// 	amap.value?.init({ latitude, longitude, zoom: 18, pitchAngle: 0, yawAngle: 0 } as mapParams)
	// })
</script>

<style>

</style>