import { colorType } from "./css/type.uts"

/**
 * theme主题类型
 */
export type Theme = {
	theme : string
	colors : colorType
}

export type BeeThemeAttr = {

	themes : Theme[]
	// 默认主题
	colors : colorType
	// 默认主题名
	currTheme : string
}